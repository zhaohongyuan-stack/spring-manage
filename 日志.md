## 2026-06-12

- 初始化后端基础配置：将 Spring Boot 调整为 3.2.x，补充 Web、PostgreSQL、MyBatis-Plus、SpringDoc、测试依赖。
- 新增 `application.yml`，配置 PostgreSQL 数据库连接、默认端口 8080、MyBatis-Plus 主键自增与下划线转驼峰、Swagger 访问路径。
- 新增基础分层包结构：`controller`、`service`、`mapper`、`entity`、`dto`、`vo`、`common`、`config`、`exception`。
- 新增统一返回结果类 `Result`、全局异常处理器 `GlobalExceptionHandler`、MyBatis-Plus 分页配置 `MybatisPlusConfig`。

## 2026-06-12 实体映射初始化

- 根据 PostgreSQL 当前表结构新增 12 个 MyBatis-Plus 实体：`Department`、`Product`、`Supplier`、`User`、`Purchase`、`PurchaseItem`、`Inbound`、`InboundItem`、`Receive`、`ReceiveItem`、`Inventory`、`InventoryRecord`。
- 为实体字段补充 `@TableName`、`@TableId`、`@TableField` 映射，复杂主表使用 `@TableField(exist = false)` 临时存储明细集合。
- 新增 MyBatis-Plus 自动填充处理器，统一填充 `createTime`、`updateTime`。

## 2026-06-12 用户基础接口

- 新增用户接口 DTO/VO：`AddUserDTO`、`UserListQueryDTO`、`AddUserVO`、`UserListVO`、`PageResultVO`，接口入参与出参与数据库实体隔离。
- 新增 `UserMapper`、`UserService`、`UserController`，实现 `POST /api/v1/user/add` 和 `GET /api/v1/user/list`。
- 补充 Validation 与 Spring Security 基础依赖，新增 `SecurityConfig` 和 BCrypt 密码加密器。
- 完善全局参数校验异常处理，并新增用户接口控制器测试。

## 2026-06-12 基础编码与权限清单

- 新增 `common.enums` 角色、状态、类型枚举，统一维护角色编码、单据状态、入库类型和库存变动类型。
- 新增基础信息模块 Mapper：`DepartmentMapper`、`SupplierMapper`、`ProductMapper`，并继续复用已有 `UserMapper`。
- 新增 `OrderNoUtil`，封装采购单、入库单、领用单编号生成规则。
- 调整 `SecurityConfig` 注释与临时放行开关，明确 JWT 延后时的临时策略和后续正式拦截切换点。
- 新增 `接口权限清单.md`，归档接口允许访问角色，并确认分页接口统一复用 `PageResultVO`。

## 2026-06-12 部门管理模块

- 新增部门接口 DTO/VO：`AddDepartmentDTO`、`DepartmentTreeVO`，完成入参校验与树形响应结构。
- 新增 `BusinessException` 并接入全局异常处理，供 Service 层统一抛出业务规则错误。
- 新增 `DepartmentService`、`DepartmentServiceImpl`、`DepartmentController`，实现 `POST /api/v1/department/add` 和 `GET /api/v1/department/tree`。
- 新增 `DepartmentControllerTest`，验证新增部门、参数校验和当前临时放行策略下的接口访问。

## 2026-06-12 供应商管理模块

- 新增供应商接口 DTO/VO：`AddSupplierDTO`、`DeleteSupplierDTO`、`SupplierQueryDTO`、`SupplierVO`，接口入参与出参与数据库实体隔离。
- 新增 `SupplierService`、`SupplierServiceImpl`、`SupplierController`，实现 `POST /api/v1/supplier/add`、`POST /api/v1/supplier/delete`、`GET /api/v1/supplier/list`。
- 供应商新增校验名称重复，删除校验供应商是否存在，分页查询支持供应商名称模糊查询并复用 `PageResultVO`。
- 新增 `SupplierControllerTest`，验证新增、删除、分页、参数校验、业务异常和当前临时放行策略。

## 2026-06-16 商品管理模块

- 新增商品接口 DTO/VO：`AddProductDTO`、`UpdateProductDTO`、`DeleteProductDTO`、`ProductQueryDTO`、`ProductVO`，接口入参与出参与数据库实体隔离。
- 新增 `ProductStatusEnum`，统一维护商品启用和禁用状态。
- 新增 `ProductService`、`ProductServiceImpl`、`ProductController`，实现 `POST /api/v1/product/add`、`GET /api/v1/product/list`、`POST /api/v1/product/update`、`POST /api/v1/product/delete`。
- 商品删除改为逻辑禁用 `product.status=0`，商品列表仅展示启用商品；新增商品不创建库存记录，库存记录留待首次入库验收时处理。
- 新增 `ProductControllerTest`，验证新增、列表、修改、逻辑删除、参数校验、业务异常和当前临时放行策略。

## 2026-06-16 采购管理模块

- 新增 Redis 采购单号生成能力，按 `purchase_no:yyyyMMdd` 使用 Redis `INCR` 生成 `CG-yyyyMMdd-三位流水` 单号。
- 新增采购接口 DTO/VO、Mapper、Service、Controller，实现采购单创建、分页列表、详情、审批、采购确认和取消接口。
- 采购单创建使用事务同时写入 `purchase` 和 `purchase_item`，金额统一使用 `BigDecimal` 计算，明细预留 `receivedQuantity`。
- 采购状态流转按 `PurchaseStatusEnum` 校验；取消采购单时严格校验是否存在任何关联入库单，存在则不允许取消。
- 本阶段按 admin 口径暂不实现接口权限、角色权限和部门负责人/采购员权限校验，相关 TODO 已写在 Controller 注释中，后续 JWT 接入后扩展。
- 新增采购单号唯一索引 SQL 脚本 `sql/20260616_add_purchase_no_unique_index.sql`，需由数据库侧执行。
- 新增 `PurchaseControllerTest` 和 `PurchaseServiceImplTest`，覆盖主要接口、状态流转和异常场景。

## 2026-06-16 入库管理模块

- 新增入库接口 DTO/VO、Mapper、Redis 入库单号服务、Service、Controller，实现入库单创建、分页列表、详情和验收确认接口。
- 入库单号使用 Redis `inbound_no:yyyyMMdd` 原子递增生成 `RK-yyyyMMdd-三位流水`，新增唯一索引 SQL 脚本 `sql/20260616_add_inbound_no_unique_index.sql`。
- 创建入库单时校验采购单状态必须为已通过或采购中，支持分批入库并校验实际入库数量不得超过剩余可入数量。
- 验收入库单时保留部门负责人业务校验，常规入库更新库存移动加权平均并写流水，食材直拨不更新库存但写负数流水。
- 验收后更新采购明细已收数量；当采购单所有商品累计收货完成时，采购单状态更新为已入库。
- 新增 `InboundControllerTest` 和 `InboundServiceImplTest`，覆盖创建、查询、验收、库存更新、食材直拨、超量校验和验收人校验。
