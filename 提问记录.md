## 2026-06-12

- 用户接口权限待确认：当前尚未定义登录认证方式、当前用户获取方式，以及“系统管理员”的角色编码。用户新增接口文档写明由系统管理员操作，用户列表由系统管理员/酒店总经理查询。后续接入 Spring Security 时，需要确认管理员角色编码是否新增为 `admin`，以及是否使用 JWT/Session。
角色编码	对应中文名称
回答：直接复用 user 表原有 role 字段，无需新增数据库字段，不改动已有的实体，后续封装为枚举类统一管理
admin	系统管理员
gm	酒店总经理
dept_head	部门负责人
dept_employee	部门员工
purchaser	采购员
warehouse_keeper	仓库管理员
finance	财务人员
采用 JWT 令牌认证，当前已完成用户模块接口开发，Spring Security 临时放行接口保证功能可用，但未实现业务要求的角色权限控制。
原因：① 未定义「系统管理员」角色编码；② 未实现登录认证体系（Session/JWT），无法识别当前登录用户及角色；项目启动输出 Security 默认密码为框架内置账号导致，属于临时状态。技术栈为 Spring Security + JWT。

处理记录：已新增 `RoleEnum` 固化全部角色编码和中文名称，`SecurityConfig` 已标注临时放行与正式拦截切换点；JWT 按当前阶段安排延后开发。