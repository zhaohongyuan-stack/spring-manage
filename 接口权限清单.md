# 接口权限清单

## 说明

- 角色编码统一引用 `RoleEnum`：`admin`、`gm`、`dept_head`、`dept_employee`、`purchaser`、`warehouse_keeper`、`finance`。
- 当前 `SecurityConfig` 处于临时放行阶段，JWT 延后开发；正式接入认证后按本清单添加权限拦截。
- 分页接口统一使用 `PageResultVO<T>`，后续不再为单个模块重复创建分页结构体。

## 基础信息模块

- `POST /api/v1/user/add`：`admin`
- `POST /api/v1/user/delete`：`admin`
- `GET /api/v1/user/list`：`admin`、`gm`
- `POST /api/v1/department/add`：`admin`
- `GET /api/v1/department/tree`：所有登录角色
- `POST /api/v1/supplier/add`：`admin`、`purchaser`
- `POST /api/v1/supplier/delete`：`admin`、`purchaser`
- `GET /api/v1/supplier/list`：`admin`、`purchaser`
- `POST /api/v1/product/add`：`admin`、`warehouse_keeper`
- `GET /api/v1/product/list`：所有登录角色
- `POST /api/v1/product/update`：`admin`、`warehouse_keeper`
- `POST /api/v1/product/delete`：`admin`、`warehouse_keeper`

## 采购管理模块

- `POST /api/v1/purchase/add`：`dept_employee`
- `GET /api/v1/purchase/list`：`admin`、`dept_head`、`purchaser`、`warehouse_keeper`、`finance`、`gm`
- `GET /api/v1/purchase/detail`：`admin`、`dept_head`、`purchaser`、`warehouse_keeper`、`finance`、`gm`
- `POST /api/v1/purchase/approve`：`dept_head`
- `POST /api/v1/purchase/cancel`：`dept_employee`、`purchaser`

## 入库管理模块

- `POST /api/v1/inbound/add`：`warehouse_keeper`
- `GET /api/v1/inbound/list`：`admin`、`warehouse_keeper`、`dept_head`、`finance`、`gm`
- `GET /api/v1/inbound/detail`：`admin`、`warehouse_keeper`、`dept_head`、`finance`、`gm`
- `POST /api/v1/inbound/confirm`：`dept_head`

## 领用出库模块

- `POST /api/v1/receive/add`：`dept_employee`
- `GET /api/v1/receive/list`：`admin`、`dept_head`、`warehouse_keeper`、`finance`、`gm`
- `GET /api/v1/receive/detail`：`admin`、`dept_head`、`warehouse_keeper`、`finance`、`gm`
- `POST /api/v1/receive/approve`：`dept_head`
- `POST /api/v1/receive/cancel`：`dept_employee`

## 库存管理模块

- `GET /api/v1/inventory/list`：`admin`、`warehouse_keeper`、`finance`、`gm`
- `GET /api/v1/inventory/warning`：`warehouse_keeper`
- `GET /api/v1/inventory/record`：`admin`、`warehouse_keeper`、`finance`、`gm`
- `POST /api/v1/inventory/init`：`admin`

## 字典模块

- `GET /api/v1/dict/categories`：所有登录角色
- `GET /api/v1/dict/roles`：所有登录角色

## 待确认

- 文档描述了“领用确认出库”业务，但当前接口文档未给出明确 URL；后续确认后补入本清单。
- 部分查询接口是否允许 `admin` 默认查看全部数据，当前按系统管理员最高权限先归档。
