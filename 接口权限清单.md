# 接口权限清单

## 说明

- 角色编码统一引用 `RoleEnum`：`admin`、`gm`、`dept_head`、`dept_employee`、`purchaser`、`warehouse_keeper`、`finance`。
- 当前 `SecurityConfig` 处于临时放行阶段，JWT 延后开发；正式接入认证后按本清单添加权限拦截。
- 分页接口统一使用 `PageResultVO<T>`。
- **字段描述约定**：`【标签】说明`；涉及下拉选择的字段需标明「调哪个接口、展示什么、提交什么」；涉及枚举的字段需列出可用值（code 与中文名）。

### 常用枚举速查

| 类型 | 可用值（提交 code / 数字，展示中文名） |
|---|---|
| **角色** `role` | `dept_employee` 部门员工、`dept_head` 部门负责人、`purchaser` 采购员、`warehouse_keeper` 仓库管理员、`finance` 财务人员、`gm` 酒店总经理（`admin` 系统管理员一般仅初始化，新增用户表单通常不选） |
| **商品分类** `category` | `客房用品`、`食材`、`工程物资`、`办公用品`、`清洁用品`、`其他`（GET `/api/v1/dict/categories`） |
| **采购单状态** `status` | `1` 待审批、`2` 已通过、`3` 采购中、`4` 已入库、`5` 已取消、`6` 已驳回 |
| **入库单状态** `status` | `1` 待验收、`2` 已验收 |
| **入库类型** `inboundType` | `1` 常规入库、`2` 食材直拨 |
| **领用单状态** `status` | `1` 待审批、`2` 待出库、`3` 已出库、`4` 已取消、`5` 已驳回 |
| **库存变动类型** `changeType` | `1` 正常入库、`2` 正常出库、`3` 食材直拨、`4` 期初、`5` 盘点 |

### 常用下拉数据源

| 前端场景 | 接口 | 展示字段 | 提交字段 |
|---|---|---|---|
| 选择部门 | GET `/api/v1/department/tree` | `deptName` | `id` → 请求体/Query 中的 `deptId` |
| 选择角色 | GET `/api/v1/dict/roles` | `name` | `code` → 请求体中的 `role` |
| 选择商品分类 | GET `/api/v1/dict/categories` | 字符串本身 | 字符串本身 → `category` |
| 选择供应商 | GET `/api/v1/supplier/list` | `supplierName` | `supplierId` |
| 选择商品 | GET `/api/v1/product/list` | `productName` | `productId` |
| 选择采购单 | GET `/api/v1/purchase/list` | `purchaseNo` | `purchaseId` |

---

## 基础信息模块

### POST /api/v1/user/add — 权限：`admin`

创建系统用户。API 使用 `account` 作为登录名（入库 `username` 列）；`phone` 由后端自动填入 `186XXXX9999`，无需前端传参。

```json
{
  "account": "zhangwei336",      // 【登录名】必填，全局唯一，建议使用工号或英文名；登录时使用此字段
  "password": "123456",          // 【密码】必填，长度 6~50；前端加密传输后再提交
  "realName": "张伟",             // 【真实姓名】必填，展示用，支持中文；用于列表/日志展示
  "role": "dept_head",           // 【角色】必填，先 GET /api/v1/dict/roles；可用枚举：dept_employee(部门员工)、dept_head(部门负责人)、purchaser(采购员)、warehouse_keeper(仓库管理员)、finance(财务人员)、gm(酒店总经理)（下拉展示中文 name，提交传 code）
  "deptId": 1                    // 【所属部门】部门员工/负责人必填；通过 GET /api/v1/department/tree 下拉选择部门（展示 deptName，提交 id）；采购员/仓管/财务/gm 等职能角色可不填
}
```

### POST /api/v1/user/delete — 权限：`admin`

```json
{
  "userId": 101                   // 【用户ID】必填，目标用户主键；逻辑禁用（status=0），非物理删除
}
```

### GET /api/v1/user/list — 权限：`admin`、`gm`

| Query 参数 | 说明 |
|---|---|
| `pageNum` | 【页码】必填，从 1 开始 |
| `pageSize` | 【每页条数】必填，默认 10，最大 100 |
| `realName` | 【真实姓名】可选，模糊匹配；不传表示不按姓名筛选 |
| `role` | 【角色编码】可选，精确匹配；可用值同「角色枚举速查」；不传表示不按角色筛选 |
| `deptId` | 【部门ID】可选，精确匹配；可先调 GET `/api/v1/department/tree` 获取部门 id；不传表示不按部门筛选 |

列表响应 `list[].account` 为登录名（原数据库 `username`）；`roleName` 为角色中文名；`phone` 为系统占位值 `186XXXX9999`，前端可不展示。

### POST /api/v1/department/add — 权限：`admin`

```json
{
  "deptName": "会议宴会部",       // 【部门名称】必填，如客房部、餐饮部；列表/树形展示用
  "parentId": 0                  // 【上级部门ID】必填，顶级部门传 0；非顶级时传上级部门 id（可先调 GET /api/v1/department/tree 选择上级）
}
```

### GET /api/v1/department/tree — 权限：所有登录角色

无请求参数。返回树形部门列表，供「选择部门」下拉/树形控件使用。

响应节点字段：`id`（提交用）、`deptName`（展示用）、`parentId`、`children`。

### POST /api/v1/supplier/add — 权限：`admin`、`purchaser`

```json
{
  "supplierName": "大连XX食品配送中心",  // 【供应商全称】必填，后续采购单下拉展示此名称
  "contactPerson": "李主管",            // 【联系人】可选
  "contactPhone": "13900139000",       // 【联系电话】可选
  "address": "大连市XX区XX路"           // 【地址】可选
}
```

### POST /api/v1/supplier/delete — 权限：`admin`、`purchaser`

```json
{
  "id": 2                         // 【供应商ID】必填，来自 GET /api/v1/supplier/list；已被采购单引用时不可删
}
```

### GET /api/v1/supplier/list — 权限：`admin`、`purchaser`

| Query 参数 | 说明 |
|---|---|
| `pageNum` | 【页码】必填，从 1 开始 |
| `pageSize` | 【每页条数】必填，默认 10 |
| `supplierName` | 【供应商名称】可选，模糊匹配；不传返回全部（分页） |

响应 `list[].supplierId` 供采购单 `supplierId` 提交；`supplierName` 供下拉展示。

### POST /api/v1/product/add — 权限：`admin`、`warehouse_keeper`

```json
{
  "productName": "瓶装洗发水",     // 【商品名称】必填，列表/下拉展示用
  "category": "客房用品",         // 【商品分类】必填，GET /api/v1/dict/categories；可用：客房用品、食材、工程物资、办公用品、清洁用品、其他（下拉展示中文，提交传字符串）
  "spec": "300ml/瓶",            // 【规格型号】可选
  "unit": "瓶",                  // 【单位】可选，如套/瓶/个
  "costPrice": 4.10,             // 【成本单价】必填，单位元，保留 2 位小数
  "warningThreshold": 30         // 【预警阈值】必填，库存低于此值触发预警
}
```

### GET /api/v1/product/list — 权限：所有登录角色

| Query 参数 | 说明 |
|---|---|
| `pageNum` | 【页码】必填 |
| `pageSize` | 【每页条数】必填 |
| `productName` | 【商品名称】可选，模糊匹配 |
| `category` | 【商品分类】可选，精确匹配；可用值同「商品分类」枚举 |

响应 `list[].productId` 供采购/领用/入库明细提交；`productName` 供下拉展示。

### POST /api/v1/product/update — 权限：`admin`、`warehouse_keeper`

```json
{
  "productId": 2,                // 【商品ID】必填，来自 GET /api/v1/product/list
  "costPrice": 4.30,             // 【成本单价】可选，传则更新
  "warningThreshold": 25         // 【预警阈值】可选，传则更新
}
```

### POST /api/v1/product/delete — 权限：`admin`、`warehouse_keeper`

```json
{
  "productId": 2                 // 【商品ID】必填，逻辑删除
}
```

---

## 采购管理模块

### POST /api/v1/purchase/add — 权限：`dept_employee`

```json
{
  "deptId": 1,                   // 【申请部门】必填，须为当前用户所属部门；GET /api/v1/department/tree 下拉选部门（展示 deptName，提交 id）
  "applyUserId": 1001,           // 【申请人】必填，当前登录用户 ID（通常从登录态获取，勿手填他人 ID）
  "requireDate": "2026-06-20",   // 【需求日期】必填，期望到货日期，格式 yyyy-MM-dd，不能早于今天
  "supplierId": 3,               // 【供应商】必填，GET /api/v1/supplier/list 下拉选供应商（展示 supplierName，提交 supplierId）
  "remark": "6月下旬客房用品补充", // 【备注】可选
  "items": [
    {
      "productId": 1,            // 【商品ID】必填，GET /api/v1/product/list 下拉选商品（展示 productName，提交 productId）
      "quantity": 100,           // 【采购数量】必填，正整数
      "unitPrice": 2.20          // 【采购单价】必填，单位元，须大于 0
    }
  ]
}
```

### GET /api/v1/purchase/list — 权限：`admin`、`dept_head`、`purchaser`、`warehouse_keeper`、`finance`、`gm`

| Query 参数 | 说明 |
|---|---|
| `pageNum` | 【页码】必填 |
| `pageSize` | 【每页条数】必填 |
| `operatorUserId` | 【操作人】必填，当前登录用户 ID（后续用于按角色过滤可见范围） |
| `status` | 【采购单状态】可选；可用：1 待审批、2 已通过、3 采购中、4 已入库、5 已取消、6 已驳回（下拉展示中文 statusName，提交传数字） |
| `purchaseNo` | 【采购单号】可选，模糊匹配，如 CG-20260610-001 |

### GET /api/v1/purchase/detail — 权限：同上

| Query 参数 | 说明 |
|---|---|
| `purchaseId` | 【采购单ID】必填，来自采购列表 `purchaseId` |

### POST /api/v1/purchase/approve — 权限：`dept_head`

```json
{
  "purchaseId": 101,             // 【采购单ID】必填，须为本部门申请且状态为 1-待审批
  "approverUserId": 1005,        // 【审批人】必填，当前登录的部门负责人用户 ID
  "approved": true,              // 【审批结果】必填，true 通过 / false 驳回（下拉：通过/驳回，提交布尔值）
  "remark": "同意采购"            // 【审批备注】驳回时必填；通过时可选
}
```

### POST /api/v1/purchase/confirm — 权限：`purchaser`

```json
{
  "purchaseId": 101,             // 【采购单ID】必填，状态须为 2-已通过（待采购）
  "purchaserUserId": 1008        // 【采购员】必填，当前登录采购员用户 ID
}
```

### POST /api/v1/purchase/cancel — 权限：`dept_employee`、`purchaser`

```json
{
  "purchaseId": 101,             // 【采购单ID】必填，状态 1/2/3 且未生成入库单时可取消
  "operatorUserId": 1001         // 【操作人】必填，申请人或采购员用户 ID
}
```

---

## 入库管理模块

### POST /api/v1/inbound/add — 权限：`warehouse_keeper`

```json
{
  "purchaseId": 101,             // 【采购单ID】必填，GET /api/v1/purchase/list 选择状态 2/3 的采购单（展示 purchaseNo，提交 purchaseId）
  "receiverUserId": 2001,        // 【收货人】必填，当前登录仓管用户 ID
  "inboundType": 1,              // 【入库类型】必填，可用：1 常规入库、2 食材直拨（下拉展示中文，提交传数字）
  "remark": "外包装损坏2套",      // 【备注】可选
  "items": [
    {
      "productId": 1,            // 【商品ID】必填，须属于所选采购单明细中的商品
      "actualQuantity": 98,      // 【实际收货数量】必填，正整数，可少于采购数量
      "batchNo": "20260601-B01", // 【批次号】可选，用于追溯
      "expiryDate": "2027-06-01" // 【有效期】可选，食材/化学品建议填写，格式 yyyy-MM-dd
    }
  ]
}
```

### GET /api/v1/inbound/list — 权限：`admin`、`warehouse_keeper`、`dept_head`、`finance`、`gm`

| Query 参数 | 说明 |
|---|---|
| `pageNum` | 【页码】必填 |
| `pageSize` | 【每页条数】必填 |
| `inboundType` | 【入库类型】可选，1 常规入库 / 2 食材直拨 |
| `status` | 【入库单状态】可选，1 待验收 / 2 已验收 |
| `purchaseNo` | 【采购单号】可选，模糊匹配 |

### GET /api/v1/inbound/detail — 权限：同上

| Query 参数 | 说明 |
|---|---|
| `inboundId` | 【入库单ID】必填，来自入库列表 `inboundId` |

### POST /api/v1/inbound/confirm — 权限：`dept_head`

```json
{
  "inboundId": 301,              // 【入库单ID】必填，状态须为 1-待验收
  "deptHeadUserId": 1005,        // 【部门负责人】必填，须为采购申请部门的 dept_head 角色用户
  "warehouseKeeperUserId": 2001  // 【仓管员】必填，当前登录仓管用户 ID（验收确认操作人）
}
```

---

## 领用出库模块

### POST /api/v1/receive/add — 权限：`dept_employee`

```json
{
  "deptId": 1,                   // 【领用部门】必填，须与申请人所属部门一致；GET /api/v1/department/tree 下拉选部门（展示 deptName，提交 id）
  "applyUserId": 1001,           // 【申请人】必填，当前登录用户 ID
  "purpose": "3楼客房补充",       // 【用途说明】必填，如「3楼客房一次性用品补充」
  "remark": "急需",              // 【备注】可选
  "items": [
    {
      "productId": 1,            // 【商品ID】必填，GET /api/v1/product/list 或库存列表选择（展示 productName，提交 productId）
      "quantity": 50             // 【领用数量】必填，正整数；出库时校验库存是否充足
    }
  ]
}
```

### GET /api/v1/receive/list — 权限：`admin`、`dept_head`、`warehouse_keeper`、`finance`、`gm`

| Query 参数 | 说明 |
|---|---|
| `pageNum` | 【页码】必填 |
| `pageSize` | 【每页条数】必填 |
| `status` | 【领用单状态】可选，1 待审批、2 待出库、3 已出库、4 已取消、5 已驳回 |
| `deptId` | 【部门ID】可选，GET `/api/v1/department/tree` 获取 id |
| `applyUserId` | 【申请人】可选，用户 ID 精确匹配 |
| `startTime` | 【开始时间】可选，申请时间区间起点，如 2026-06-01T00:00:00 |
| `endTime` | 【结束时间】可选，申请时间区间终点 |

### GET /api/v1/receive/detail — 权限：同上

| Query 参数 | 说明 |
|---|---|
| `receiveId` | 【领用单ID】必填，来自领用列表 `receiveId` |

### POST /api/v1/receive/approve — 权限：`dept_head`

```json
{
  "receiveId": 201,              // 【领用单ID】必填，须为本部门申请且状态为 1-待审批
  "approverUserId": 1005,        // 【审批人】必填，当前登录部门负责人用户 ID
  "approved": true,              // 【审批结果】必填，true 通过 / false 驳回
  "remark": "同意领用"            // 【审批备注】驳回时必填
}
```

### POST /api/v1/receive/confirm — 权限：`warehouse_keeper`

```json
{
  "receiveId": 201,              // 【领用单ID】必填，状态须为 2-待出库
  "delivererUserId": 1010,       // 【发货人】必填，当前登录仓管用户 ID
  "remark": "已全部发货"          // 【备注】可选
}
```

### POST /api/v1/receive/cancel — 权限：`dept_employee`

```json
{
  "receiveId": 201,              // 【领用单ID】必填，状态 1-待审批 或 2-待出库 时可取消
  "operatorUserId": 1001         // 【操作人】必填，申请人用户 ID
}
```

---

## 库存管理模块

### GET /api/v1/inventory/list — 权限：`admin`、`warehouse_keeper`、`finance`、`gm`

| Query 参数 | 说明 |
|---|---|
| `pageNum` | 【页码】必填 |
| `pageSize` | 【每页条数】必填 |
| `productName` | 【商品名称】可选，模糊匹配 |
| `category` | 【商品分类】可选，可用值同「商品分类」枚举 |

### GET /api/v1/inventory/warning — 权限：`warehouse_keeper`

无必填 Query。返回库存低于预警阈值的商品及 `suggestPurchase` 建议采购量，供仓管发起补货/采购参考。

### GET /api/v1/inventory/record — 权限：`admin`、`warehouse_keeper`、`finance`、`gm`

| Query 参数 | 说明 |
|---|---|
| `pageNum` | 【页码】必填 |
| `pageSize` | 【每页条数】必填 |
| `productId` | 【商品ID】可选，GET `/api/v1/product/list` 获取 |
| `changeType` | 【变动类型】可选，1 正常入库、2 正常出库、3 食材直拨、4 期初、5 盘点 |
| `startTime` | 【开始时间】可选 |
| `endTime` | 【结束时间】可选 |

### POST /api/v1/inventory/init — 权限：`admin`

期初库存初始化（详见 `测试开发使用版本.md`）。

---

## 字典模块

### GET /api/v1/dict/categories — 权限：所有登录角色

无参数。返回商品分类字符串数组：`["客房用品","食材","工程物资","办公用品","清洁用品","其他"]`；商品表单下拉直接使用返回值。

### GET /api/v1/dict/roles — 权限：所有登录角色

无参数。返回 `[{"code":"dept_employee","name":"部门员工"}, {"code":"dept_head","name":"部门负责人"}, ...]`；用户表单角色下拉展示 `name`，提交 `code`。

---

## 待确认

- 部分查询接口是否允许 `admin` 默认查看全部数据，当前按系统管理员最高权限先归档。
