# 测试开发使用版本

> **字段说明约定**：请求/响应 JSON 示例中，行尾 `// 【标签】说明` 与 Swagger、`接口权限清单.md` 中 `@Schema(description=...)` 保持一致。完整全接口字段说明见 `接口权限清单.md`。

## 库存管理

## 入库

## 采购

## 领用









# 基础信息模块

```SQL
-- 用户表
CREATE TABLE `user` (
  `id` bigint PRIMARY KEY AUTO_INCREMENT,
  `username` varchar(50) NOT NULL COMMENT '登录名',
  `password` varchar(255) NOT NULL COMMENT '加密密码',
  `real_name` varchar(50) NOT NULL COMMENT '真实姓名',
  `role` varchar(20) NOT NULL COMMENT '角色：dept_employee/dept_head/purchaser/warehouse_keeper/finance/gm',
  `dept_id` bigint COMMENT '所属部门ID（员工/负责人必填）',
  `phone` varchar(20),
  `status` tinyint DEFAULT 1 COMMENT '1启用 0禁用',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 部门表
CREATE TABLE `department` (
  `id` bigint PRIMARY KEY AUTO_INCREMENT,
  `dept_name` varchar(100) NOT NULL COMMENT '部门名称，如客房部、餐饮部',
  `parent_id` bigint DEFAULT 0 COMMENT '上级部门ID，用于层级',
  `status` tinyint DEFAULT 1,
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP
);

-- 供应商表
CREATE TABLE `supplier` (
  `id` bigint PRIMARY KEY AUTO_INCREMENT,
  `supplier_name` varchar(200) NOT NULL,
  `contact_person` varchar(50),
  `contact_phone` varchar(20),
  `address` varchar(500),
  `status` tinyint DEFAULT 1,
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP
);

-- 商品表
CREATE TABLE `product` (
  `id` bigint PRIMARY KEY AUTO_INCREMENT,
  `product_name` varchar(200) NOT NULL,
  `category` varchar(50) NOT NULL COMMENT '客房用品/食材/工程物资/办公用品等',
  `spec` varchar(200) COMMENT '规格型号',
  `unit` varchar(20) COMMENT '单位，如套/瓶/个',
  `cost_price` decimal(16,2) NOT NULL COMMENT '成本单价',
  `warning_threshold` int NOT NULL DEFAULT 0 COMMENT '预警阈值（最低库存量）',
  `status` tinyint DEFAULT 1,
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

## 接口1 新增用户

POST  /api/v1/user/add

接口描述：创建系统用户，分配角色和所属部门。请求体使用 **account** 作为登录名（数据库仍存 `username` 列）；**phone 无需传参**，后端自动填入 `186XXXX9999`。`realName` 用于操作日志；`role` 决定权限；部门员工/负责人必须指定 `deptId`。

```JSON
{
  "account": "zhangwei336",      // 【登录名】必填，全局唯一，建议使用工号或英文名
  "password": "123456",          // 【密码】必填，前端加密传输
  "realName": "张伟",             // 【真实姓名】必填，展示用，支持中文
  "role": "dept_head",           // 【角色】必填，先 GET /api/v1/dict/roles；可用：dept_employee(部门员工)、dept_head(部门负责人)、purchaser(采购员)、warehouse_keeper(仓库管理员)、finance(财务人员)、gm(酒店总经理)（下拉展示中文 name，提交 code）
  "deptId": 1                    // 【所属部门】部门员工/负责人必填；GET /api/v1/department/tree 下拉选部门（展示 deptName，提交 id）；职能角色可不填
}
```

响应示例：返回新增用户的ID。

```Plain Text
{
  "code": 200,
  "message": "新增成功",
  "data": { "userId": 101 }
}
```

## 接口2 用户列表查询

系统管理员、酒店总经理，分页查询用户，支持按姓名、角色、部门筛选

接口：`GET     /api/v1/user/list`

请求参数说明（Query）

```Plain Text
pageNum      // 【页码】必填，从1开始
pageSize     // 【每页条数】必填，默认10
realName     // 【真实姓名】可选，模糊匹配
role         // 【角色编码】可选，精确匹配
deptId       // 【部门ID】可选，精确匹配
```

响应示例：

```JSON
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "total": 2,
    "pageNum": 1,
    "pageSize": 10,
    "list": [
      {
        "userId": 101,
        "account": "zhangwei336",
        "realName": "张伟",
        "role": "dept_head",
        "roleName": "部门负责人",
        "deptId": 1,
        "deptName": "客房部",
        "phone": "186XXXX9999",
        "status": 1,
        "createTime": "2026-06-10 09:00:00"
      }
    ]
  }
}
```

## 接口3 新增部门

接口：`POST /api/v1/department/add`

系统管理员添加酒店部门，如客房部、餐饮部、会议宴会部、工程部、后勤部、仓库等。`parentId` 支持树形结构（如“餐饮部”下分“中厨房”“西厨房”）。

请求参数

```Plain Text
{
  "deptName": "会议宴会部",
  "parentId": 0
}
```

响应

```JSON
{
  "code": 200,
  "message": "新增成功",
  "data": {
    "id": 5
  }
}
```

## 接口4 部门列表查询

所有人可以查询所有部门，以树形结构返回，方便前端展示组织架构。

接口：`GET /api/v1/department/tree`

请求参数：无

响应

```JSON
{
  "code": 200,
  "message": "操作成功",
  "data": [
    {
      "id": 1,
      "deptName": "客房部",
      "parentId": 0,
      "children": []
    },
    {
      "id": 2,
      "deptName": "餐饮部",
      "parentId": 0,
      "children": [
        {
          "id": 5,
          "deptName": "中餐厨房",
          "parentId": 2,
          "children": []
        }
      ]
    }
  ]
}
```

## 接口 5新增供应商

采购员、系统管理员可以录入合作供应商信息，用于采购单关联

接口地址：`POST /api/v1/supplier/add`

填入供应商全称，联系人，联系电话，地址

请求参数

```JSON
{
  "supplierName": "大连XX食品配送中心",
  "contactPerson": "李主管",
  "contactPhone": "13900139000",
  "address": "大连市XX区XX路"
}
```

响应返回供应商ID

```Plain Text
{
  "code": 200,
  "message": "新增成功",
  "data": {
    "id": 2
  }
}
```

删除供应商

接口地址：POST /api/v1/supplier/delete

请求参数（Body）：

```JSON
{
  "id": 2
}
```

响应格式：

```JSON
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```





## 接口6 供应商列表查询

接口：`GET /api/v1/supplier/list`

分页查询供应商

请求参数（Query）：

```JSON
pageNum当前页
pageSize每页大小非必传 默认10
supplierName非必填
```

响应示例

```JSON
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "total": 5,
    "pageNum": 1,
    "pageSize": 10,
    "list": [
      {
        "supplierId": 1,
        "supplierName": "沈阳XX酒店用品有限公司",
        "contactPerson": "王经理",
        "contactPhone": "13800138000",
        "address": "沈阳市XX区",
        "status": 1,
        "createTime": "2026-06-01 09:00:00"
      }
    ]
  }
}
```

## 接口7 新增商品

系统管理员或仓库管理员录入物资商品信息，设定成本单价和预警阈值（最低库存量）。`category` 从预定义分类中选择（客房用品、食材、工程物资、办公用品等）。

接口：`POST /api/v1/product/add`

请求参数说明

```Plain Text

productName（string）商品名称，如“一次性牙具套装”
category（string）分类，见数据字典
spec（string）规格，如“6g牙膏+软毛牙刷”
unit（string）单位，如“套”、“瓶”
costPrice（decimal）成本单价（元），保留2位小数
warningThreshold（int）预警阈值，库存低于此值则预警
```

请求体：

```JSON
{
  "productName": "瓶装洗发水",
  "category": "客房用品",
  "spec": "300ml/瓶",
  "unit": "瓶",
  "costPrice": 4.10,
  "warningThreshold": 30
}
```

响应返回商品ID

## 接口8 商品列表查询

所有角色分页查询商品，支持名称、分类筛选。

接口：`GET /api/v1/product/list`

请求参数说明

```Plain Text
pageNum当前页
pageSize页大小
productName用于糊匹配
category（string）非必填，精确匹配分类
```

响应示例：

```JSON
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "total": 2,
    "pageNum": 1,
    "pageSize": 10,
    "list": [
      {
        "productId": 1,
        "productName": "一次性牙具套装",
        "category": "客房用品",
        "spec": "6g牙膏+软毛牙刷",
        "unit": "套",
        "costPrice": 2.20,
        "warningThreshold": 50,
        "status": 1,
        "createTime": "2026-06-01 08:00:00"
      }
    ]
  }
}
```



## 接口9 修改商品信息

接口地址：`POST /api/v1/product/update`

系统管理员、仓库管理员更新商品信息（价格、预警阈值等）

请求体

```Plain Text
{
  "productId": 2,
  "costPrice": 4.30,
  "warningThreshold": 25
}
```

响应

```Plain Text
{
  "code": 200,
  "message": "成功",
  "data": {
  }
}
```

接口10 删除商品

接口地址：`POST /api/v1/product/delete`

请求体

```Plain Text
{
  "productId": 2
}
```

响应

```Plain Text
{
  "code": 200,
  "message": "成功",
  "data": {
  }
}
```



# 采购管理模块

数据库表

```SQL
-- 采购单表（合并采购申请与采购订单）
CREATE TABLE `purchase` (
  `id` bigint PRIMARY KEY AUTO_INCREMENT,
  `purchase_no` varchar(50) NOT NULL COMMENT '采购单号，规则：CG-YYYYMMDD-序号',
  `dept_id` bigint NOT NULL COMMENT '申请部门ID',
  `apply_user_id` bigint NOT NULL COMMENT '申请人用户ID',
  `apply_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '申请时间',
  `require_date` date NOT NULL COMMENT '需求日期（期望到货日期）',
  `supplier_id` bigint NOT NULL COMMENT '供应商ID',
  `total_amount` decimal(16,2) NOT NULL COMMENT '采购总金额',
  `purchase_status` tinyint NOT NULL DEFAULT 1 COMMENT '状态：1待审批 2已通过（待采购） 3采购中 4已入库 5已取消 6已驳回',
  `approver_id` bigint DEFAULT NULL COMMENT '审批人用户ID（部门负责人）',
  `approve_time` datetime DEFAULT NULL COMMENT '审批时间',
  `purchaser_id` bigint DEFAULT NULL COMMENT '采购员用户ID（确认采购的人）',
  `purchase_confirm_time` datetime DEFAULT NULL COMMENT '采购确认时间',
  `remark` varchar(500) DEFAULT NULL COMMENT '备注',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 采购明细表
CREATE TABLE `purchase_item` (
  `id` bigint PRIMARY KEY AUTO_INCREMENT,
  `purchase_id` bigint NOT NULL COMMENT '采购单ID',
  `product_id` bigint NOT NULL COMMENT '商品ID',
  `product_name` varchar(200) NOT NULL COMMENT '冗余商品名称',
  `quantity` int NOT NULL COMMENT '采购数量',
  `unit_price` decimal(16,2) NOT NULL COMMENT '采购单价',
  `amount` decimal(16,2) NOT NULL COMMENT '小计金额',
  `received_quantity` int DEFAULT 0 COMMENT '已收货数量（用于入库校验）'
);
```

## 创建采购申请

部门员工dept\_employee发起物资采购申请，填写申请部门、需求日期、供应商、采购明细。系统自动生成采购单号（规则：CG\-年月日\-当天流水号），初始状态为 `1-待审批`。总金额根据明细自动计算。

接口地址：`POST /api/v1/purchase/add`

请求参数说明：

请求体示例

```JSON
{
  "deptId": 1,
  "applyUserId": 1001,
  "requireDate": "2026-06-20",
  "supplierId": 3,
  "remark": "6月下旬客房用品补充",
  "items": [
    {
      "productId": 1,
      "quantity": 100,
      "unitPrice": 2.20
    },
    {
      "productId": 2,
      "quantity": 50,
      "unitPrice": 4.30
    }
  ]
}
```

响应示例

```JSON
{
  "code": 200,
  "message": "申请成功",
  "data": {
    "purchaseId": 101,
    "purchaseNo": "CG-20260610-001"
  }
}
```

## 采购单列表查询

部门负责人（看本部门）、采购员（看所有待采购/采购中）、仓库管理员、财务、总经理分页查询采购单

根据角色权限返回采购单列表。部门负责人只显示其部门下的申请；采购员可查看所有状态为2/3/4/5的单据；管理员可见全部。

接口：`GET /api/v1/purchase/list`

请求参数说明

响应

```JSON
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "total": 5,
    "pageNum": 1,
    "pageSize": 10,
    "list": [
      {
        "purchaseId": 101,
        "purchaseNo": "CG-20260610-001",
        "deptId": 1,
        "deptName": "客房部",
        "applyUserId": 1001,
        "applyUserRealName": "张三",
        "applyTime": "2026-06-10 09:00:00",
        "requireDate": "2026-06-20",
        "supplierId": 3,
        "supplierName": "沈阳XX酒店用品公司",
        "totalAmount": 475.00,
        "status": 2,
        "statusName": "已通过（待采购）",
        "items": [
          {
            "productId": 1,
            "productName": "一次性牙具套装",
            "quantity": 100,
            "unitPrice": 2.20,
            "amount": 220.00
          }
        ]
      }
    ]
  }
}
```

## 采购单详情查询

查看单个采购单完整信息，根据采购单ID返回详细信息，包括审批人、采购确认人、时间等

接口：`GET /api/v1/purchase/detail`

请求参数

响应示例

```JSON
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "purchaseId": 101,
    "purchaseNo": "CG-20260610-001",
    "deptId": 1,
    "deptName": "客房部",
    "applyUserId": 1001,
    "applyUserRealName": "张三",
    "applyTime": "2026-06-10 09:00:00",
    "requireDate": "2026-06-20",
    "supplierId": 3,
    "supplierName": "沈阳XX酒店用品公司",
    "totalAmount": 475.00,
    "status": 2,
    "approverId": 1005,
    "approverRealName": "李经理",
    "approveTime": "2026-06-10 10:00:00",
    "purchaserId": null,
    "purchaseConfirmTime": null,
    "remark": "6月下旬客房用品补充",
    "items": [
      {
        "productId": 1,
        "productName": "一次性牙具套装",
        "quantity": 100,
        "unitPrice": 2.20,
        "amount": 220.00,
        "receivedQuantity": 0
      },
      {
        "productId": 2,
        "productName": "瓶装洗发水",
        "quantity": 50,
        "unitPrice": 4.30,
        "amount": 215.00,
        "receivedQuantity": 0
      }
    ],
    "createTime": "2026-06-10 09:00:00"
  }
}
```

## 采购单审批

部门负责人（dept\_head），且只能审批本部门申请，审批采购申请（通过或驳回）负责人传入采购单ID和审批结果。通过时状态变为 2\-已通过（待采购），并记录审批人和审批时间；驳回时状态变为 6\-已驳回，申请人可修改后重新提交。

接口地址：`POST /api/v1/purchase/approve`

请求参数说明：

请求体

```JSON
{
  "purchaseId": 101,
  "approverUserId": 1005,
  "approved": true,
  "remark": "同意采购"
}
```

响应示例

```Plain Text
{
  "code": 200,
  "message": "审批通过",
  "data": null
}
```

## 采购确认（开始采购）

采购员确认已联系供应商或已下单，状态从“已通过”变为“采购中”。此步骤便于追踪采购进度。只有状态为 2的单据可以执行此操作。

请求参数说明

请求体

```Plain Text
{
  "purchaseId": 101,
  "purchaserUserId": 1008
}
```

响应

```Plain Text
{
  "code": 200,
  "message": "已确认为采购中",
  "data": null
}
```

## 取消采购单（未入库前）

申请人（部门员工）或采购员，仅当状态为 1\-待审批 或 2\-已通过 或 3\-采购中 且未生成入库单时可主动取消。

取消后状态变为 5\-已取消（区别于审批驳回的 6\-已驳回）。

接口地址：`POST /api/v1/purchase/cancel`

请求体

```Plain Text
{
  "purchaseId": 101,
  "operatorUserId": 1001
}
```

响应

```Plain Text
{
  "code": 200,
  "message": "已取消",
  "data": null
}
```

# 入库管理模块

数据库表结构

```SQL
-- 入库单表
CREATE TABLE `inbound` (
  `id` bigint PRIMARY KEY AUTO_INCREMENT,
  `inbound_no` varchar(50) NOT NULL COMMENT '入库单号，规则：RK-YYYYMMDD-序号',
  `purchase_id` bigint NOT NULL COMMENT '关联采购单ID',
  `supplier_id` bigint NOT NULL COMMENT '供应商ID（从采购单带入）',
  `receiver_id` bigint NOT NULL COMMENT '仓库管理员用户ID（创建入库单的人）',
  `inbound_type` tinyint NOT NULL COMMENT '入库类型：1常规入库 2食材直拨',
  `total_amount` decimal(16,2) NOT NULL COMMENT '本次入库总金额（按实际收货数量计算）',
  `inbound_status` tinyint NOT NULL DEFAULT 1 COMMENT '状态：1待验收 2已验收',
  `dept_head_id` bigint DEFAULT NULL COMMENT '部门负责人用户ID（验收人）',
  `accept_time` datetime DEFAULT NULL COMMENT '验收时间',
  `remark` varchar(500) DEFAULT NULL COMMENT '备注',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 入库明细表
CREATE TABLE `inbound_item` (
  `id` bigint PRIMARY KEY AUTO_INCREMENT,
  `inbound_id` bigint NOT NULL,
  `product_id` bigint NOT NULL,
  `product_name` varchar(200) NOT NULL,
  `order_quantity` int NOT NULL COMMENT '采购单数量',
  `actual_quantity` int NOT NULL COMMENT '实际收货数量',
  `unit_price` decimal(16,2) NOT NULL COMMENT '采购单价',
  `amount` decimal(16,2) NOT NULL COMMENT '实际收货金额',
  `batch_no` varchar(100) DEFAULT NULL COMMENT '批次号（可选，用于追溯）',
  `expiry_date` date DEFAULT NULL COMMENT '有效期（食材/化学品）'
);
```

## 创建入库单

接口：`POST /api/v1/inbound/add`

仓库管理员选择已通过（或采购中）的采购单，填写实际收货数量（可少于订单数），选择入库类型。系统自动生成入库单号，状态为 `1-待验收`。此时尚未影响库存。

参数说明

请求体

```JSON
{
  "purchaseId": 101,
  "receiverUserId": 2001,
  "inboundType": 1,
  "remark": "外包装损坏2套，已退回",
  "items": [
    {
      "productId": 1,
      "actualQuantity": 98,
      "batchNo": "20260601-B01",
      "expiryDate": "2027-06-01"
    },
    {
      "productId": 2,
      "actualQuantity": 50,
      "batchNo": "20260501-C02",
      "expiryDate": "2026-12-01"
    }
  ]
}
```

响应示例 返回入库单ID和单号

```JSON
{
  "code": 200,
  "message": "入库单创建成功，等待验收",
  "data": {
    "inboundId": 301,
    "inboundNo": "RK-20260610-001"
  }
}
```

## 入库单列表查询

仓库管理员、部门负责人、财务、总经理，分页查询入库单，支持按类型、状态、采购单号等筛选

接口：`GET /api/v1/inbound/list`

请求参数说明

请求体

```JSON
{
  "page_num": 1,
  "page_size": 10,
  "inbound_type": 1,
  "status": 1,
  "purchase_no": "CG-20260608-001"
}
```

响应示例

```JSON
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "total": 3,
    "pageNum": 1,
    "pageSize": 10,
    "list": [
      {
        "inboundId": 301,
        "inboundNo": "RK-20260610-001",
        "purchaseId": 101,
        "purchaseNo": "CG-20260610-001",
        "supplierName": "沈阳XX酒店用品公司",
        "receiverName": "李四",
        "inboundType": 1,
        "inboundTypeName": "常规入库",
        "totalAmount": 430.60,
        "status": 1,
        "statusName": "待验收",
        "createTime": "2026-06-10 14:00:00"
      }
    ]
  }
}
```

## 入库单详情查询

查看入库单完整信息（含明细、验收信息）

接口地址：`GET /api/v1/inbound/detail`

请求参数（Query）：`inboundId=301`

响应示例

```JSON
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "inboundId": 301,
    "inboundNo": "RK-20260610-001",
    "purchaseId": 101,
    "purchaseNo": "CG-20260610-001",
    "supplierId": 3,
    "supplierName": "沈阳XX酒店用品公司",
    "receiverId": 2001,
    "receiverName": "李四",
    "inboundType": 1,
    "totalAmount": 430.60,
    "status": 1,
    "deptHeadId": null,
    "deptHeadName": null,
    "acceptTime": null,
    "remark": "外包装损坏2套，已退回",
    "items": [
      {
        "productId": 1,
        "productName": "一次性牙具套装",
        "orderQuantity": 100,
        "actualQuantity": 98,
        "unitPrice": 2.20,
        "amount": 215.60,
        "batchNo": "20260601-B01",
        "expiryDate": "2027-06-01"
      },
      {
        "productId": 2,
        "productName": "瓶装洗发水",
        "orderQuantity": 50,
        "actualQuantity": 50,
        "unitPrice": 4.30,
        "amount": 215.00,
        "batchNo": "20260501-C02",
        "expiryDate": "2026-12-01"
      }
    ],
    "createTime": "2026-06-10 14:00:00"
  }
}
```

## 入库单验收确认

接口地址：`POST /api/v1/inbound/confirm`

校验入库单状态为“1\-待验收”。校验传入的部门负责人用户ID确实属于申请部门（或物资使用部门）的负责人。

根据 inboundType 执行不同逻辑：

常规入库：增加对应商品库存（按批次，若无批次则累加）；生成库存流水，change\_type=1（正常入库），relatedNo 为入库单号。

食材直拨：不增加库存，直接生成库存流水，change\_type=3（食材直拨），数量为负数，成本计入部门（通过关联采购单的部门）。

更新入库单状态为 2\-已验收，记录 dept\_head\_id 和 accept\_time。

更新关联的采购单状态为 4\-已入库

请求参数说明

请求体

```Plain Text
{
  "inboundId": 301,
  "deptHeadUserId": 1005,
  "warehouseKeeperUserId": 2001
}
```

响应

```JSON
{
  "code": 200,
  "message": "验收成功，库存已更新",
  "data": null
}
```

补货通知

补货通知由库存管理模块的预警接口实现，仓库管理员需要主动查看低于安全库存的商品并发起采购申请。是由库存预警接口提供数据。

仓库管理员可调用获取低于预警阈值的商品列表，然后通知相关部门或直接发起采购申请。

`接口 GET /api/v1/inventory/warning`

# 领用出库管理模块

数据库表结构

```SQL
-- 领用单表
CREATE TABLE `receive` (
  `id` bigint PRIMARY KEY AUTO_INCREMENT,
  `receive_no` varchar(50) NOT NULL COMMENT '领用单号，规则：LY-YYYYMMDD-序号',
  `dept_id` bigint NOT NULL COMMENT '领用部门ID',
  `apply_user_id` bigint NOT NULL COMMENT '申请人用户ID',
  `apply_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '申请时间',
  `purpose` varchar(200) NOT NULL COMMENT '用途说明，如“3楼客房补充”',
  `total_amount` decimal(16,2) NOT NULL COMMENT '领用总金额',
  `receive_status` tinyint NOT NULL DEFAULT 1 COMMENT '状态：1待审批 2待出库 3已出库 4已取消 5 已经驳回',
  `approver_id` bigint DEFAULT NULL COMMENT '审批人用户ID（部门负责人）',
  `approve_time` datetime DEFAULT NULL COMMENT '审批时间',
  `deliverer_id` bigint DEFAULT NULL COMMENT '出库发货人用户ID（仓库管理员）',
  `deliver_time` datetime DEFAULT NULL COMMENT '出库确认时间',
  `remark` varchar(500) DEFAULT NULL COMMENT '备注',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 领用明细表
CREATE TABLE `receive_item` (
  `id` bigint PRIMARY KEY AUTO_INCREMENT,
  `receive_id` bigint NOT NULL COMMENT '领用单ID',
  `product_id` bigint NOT NULL COMMENT '商品ID',
  `product_name` varchar(200) NOT NULL COMMENT '冗余商品名称',
  `quantity` int NOT NULL COMMENT '领用数量',
  `unit_price` decimal(16,2) NOT NULL COMMENT '当时成本单价（从库存获取）',
  `amount` decimal(16,2) NOT NULL COMMENT '小计金额'
);
```

## 创建领用申请

员工（dept\_employee）发起物资领用申请，申请人填写领用部门（必须与本人所属部门一致）、用途、领用明细。系统自动生成领用单号（LY\-年月日\-流水号），初始状态为 `1-待审批`。总金额根据明细中的数量和实时成本单价自动计算（从库存表获取当前成本单价，采用移动加权平均或最后一次入库价）。



接口：`POST /api/v1/receive/add`

请求参数

请求体示例

```JSON
{
  "deptId": 1,
  "applyUserId": 1001,
  "purpose": "3楼客房一次性用品补充",
  "remark": "急需，请尽快审批",
  "items": [
    {
      "productId": 1,
      "quantity": 50
    },
    {
      "productId": 2,
      "quantity": 20
    }
  ]
}
```

响应

```JSON
{
  "code": 200,
  "message": "申请成功",
  "data": {
    "receiveId": 201,
    "receiveNo": "LY-20260610-001"
  }
}
```

## 领用单列表查询

部门负责人（看本部门）、仓库管理员（看所有待出库/已出库）、财务、总经理根据角色权限返回领用单列表。部门负责人只显示其部门下的申请；仓库管理员可查看所有状态为1/2/3的单据。

接口地址：`GET /api/v1/receive/list`

请求参数说明

请求

```JSON
{
  "page_num": 1,
  "page_size": 10,
  "status": 1,
  "dept_id": 1,
  "apply_user_id": 101,
  "start_time": "2026-06-01 00:00:00",
  "end_time": "2026-06-10 23:59:59"
}
```

响应示例

```JSON
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "total": 3,
    "pageNum": 1,
    "pageSize": 10,
    "list": [
      {
        "receiveId": 201,
        "receiveNo": "LY-20260610-001",
        "deptId": 1,
        "deptName": "客房部",
        "applyUserId": 1001,
        "applyUserRealName": "张三",
        "applyTime": "2026-06-10 09:00:00",
        "purpose": "3楼客房一次性用品补充",
        "totalAmount": 196.00,
        "status": 2,
        "statusName": "待出库"
      }
    ]
  }
}
```

## 领用单详情查询

根据领用单ID返回详细信息，包括审批人、发货人、时间等。

接口：`GET /api/v1/receive/detail`

请求参数说明

响应

```JSON
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "receiveId": 201,
    "receiveNo": "LY-20260610-001",
    "deptId": 1,
    "deptName": "客房部",
    "applyUserId": 1001,
    "applyUserRealName": "张三",
    "applyTime": "2026-06-10 09:00:00",
    "purpose": "3楼客房一次性用品补充",
    "totalAmount": 196.00,
    "status": 2,
    "approverId": 1005,
    "approverRealName": "李经理",
    "approveTime": "2026-06-10 09:30:00",
    "delivererId": null,
    "deliverTime": null,
    "remark": "急需，请尽快审批",
    "items": [
      {
        "productId": 1,
        "productName": "一次性牙具套装",
        "quantity": 50,
        "unitPrice": 2.20,
        "amount": 110.00
      },
      {
        "productId": 2,
        "productName": "瓶装洗发水",
        "quantity": 20,
        "unitPrice": 4.30,
        "amount": 86.00
      }
    ],
    "createTime": "2026-06-10 09:00:00"
  }
}
```

## 领用单审批

负责人传入领用单ID和审批结果。通过时状态变为 2\-待出库，记录审批人和审批时间；驳回时状态变为 5\-驳回，申请人可修改后重新提交。部门负责人审批领用申请（通过或驳回）

接口：`POST /api/v1/receive/approve`

请求参数说明

请求体示例

```JSON
{
  "receiveId": 201,
  "approverUserId": 1005,
  "approved": true,
  "remark": "同意领用"
}
```

响应

```JSON
{
  "code": 200,
  "message": "审批通过",
  "data": null
}
```

```JSON
{
  "purchaseId": 101,
  "approverUserId": 1005,
  "approved": false,
  "remark": "采购数量超出季度预算，请重新申请"
}
```

请求示例（驳回场景）

```JSON
{
  "receiveId": 201,
  "approverUserId": 1005,
  "approved": false,
  "remark": "该物资库存充足，暂无需领用"
}
```

错误响应（缺少驳回理由）

```JSON
{
  "code": 400,
  "message": "驳回时必须填写驳回理由",
  "data": null
}
```





修改：

部门负责人审批本部门的采购申请。

若 approved = true，状态变为 2\-已通过（待采购）。

若 approved = false，状态变为 5\-已驳回，且必须填写驳回理由（remark 字段），否则返回参数校验错误。



## 领用确认出库

仓库管理员确认出库，扣减库存，生成出库流水。

仓库管理员根据领用单发货，确认出库。系统执行：

1. 校验当前库存是否足够（每个商品库存 ≥ 领用数量）。

2. 扣减对应商品的库存数量。

3. 生成一条库存流水，change\_type 为 2\-正常出库，relatedNo 填入领用单号。

4. 将领用单状态更新为 3\-已出库，记录发货人和发货时间。

5. 将本次领用金额计入对应部门成本。

请求参数说明

请求体

```Plain Text
{
  "receiveId": 201,
  "delivererUserId": 1010,
  "remark": "已全部发货"
}
```

响应

```JSON
{
  "code": 200,
  "message": "出库成功",
  "data": null
}
```

```Plain Text
{
  "code": 400,
  "message": "商品【一次性牙具套装】库存不足，当前库存30，领用数量50",
  "data": null
}
```

## 取消领用单

申请人可取消领用申请，仅当状态为 `1-待审批` 或 `2-待出库` 时可取消。取消后状态变为 `4-已取消`。

接口：`POST /api/v1/receive/cancel`

申请人可主动取消领用申请，仅当状态为 `1-待审批` 或 `2-待出库` 时可取消。

取消后状态变为 `4-已取消`（区别于审批驳回的 `5-已驳回`）。

并在接口描述中强调：此接口仅用于主动取消，不影响驳回记录。

请求参数

请求体

```Plain Text
{
  "receiveId": 201,
  "operatorUserId": 1001
}
```

响应

```Plain Text
{
  "code": 200,
  "message": "已取消",
  "data": null
}
```

# 库存管理模块

数据库表结构

```SQL
-- 库存表（每个商品一条记录）
CREATE TABLE `inventory` (
  `product_id` bigint PRIMARY KEY COMMENT '商品ID，关联product表',
  `stock_quantity` int NOT NULL DEFAULT 0 COMMENT '当前库存数量',
  `unit_cost` decimal(16,2) NOT NULL DEFAULT 0.00 COMMENT '当前成本单价（移动加权平均）',
  `total_cost` decimal(16,2) NOT NULL DEFAULT 0.00 COMMENT '当前库存总金额（stock_quantity * unit_cost）',
  `last_update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间'
);

-- 库存流水表（记录每一次库存变动）
CREATE TABLE `inventory_record` (
  `id` bigint PRIMARY KEY AUTO_INCREMENT,
  `product_id` bigint NOT NULL COMMENT '商品ID',
  `product_name` varchar(200) NOT NULL COMMENT '冗余商品名称',
  `change_type` tinyint NOT NULL COMMENT '变动类型：1正常入库 2正常出库 3食材直拨 4期初初始化 5盘点调整',
  `change_quantity` int NOT NULL COMMENT '变动数量（入库为正，出库/直拨为负）',
  `before_quantity` int NOT NULL COMMENT '变动前库存',
  `after_quantity` int NOT NULL COMMENT '变动后库存',
  `related_no` varchar(50) DEFAULT NULL COMMENT '关联单据号（入库单号/领用单号/盘点单号）',
  `operator_id` bigint NOT NULL COMMENT '操作人用户ID',
  `operator_name` varchar(50) NOT NULL COMMENT '操作人姓名',
  `remark` varchar(500) DEFAULT NULL COMMENT '备注',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '操作时间'
);
```

## 库存查询

接口：`GET /api/v1/inventory/list`

页查询当前所有商品的库存数量、成本单价和总金额，支持按商品名称模糊搜索，返回库存列表。库存金额 = 库存数量 × 当前成本单价

请求参数说明

请求体

```JSON
{
  "page_num": 1,
  "page_size": 10,
  "product_name": "牙具",
  "category": "客房用品"
}
```

响应

```JSON
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "total": 2,
    "pageNum": 1,
    "pageSize": 10,
    "list": [
      {
        "productId": 1,
        "productName": "一次性牙具套装",
        "category": "客房用品",
        "spec": "6g牙膏+软毛牙刷",
        "unit": "套",
        "stockQuantity": 48,
        "unitCost": 2.20,
        "totalCost": 105.60,
        "warningThreshold": 50,
        "lastUpdateTime": "2026-06-12 10:30:00"
      },
      {
        "productId": 2,
        "productName": "瓶装洗发水",
        "category": "客房用品",
        "spec": "300ml/瓶",
        "unit": "瓶",
        "stockQuantity": 25,
        "unitCost": 4.30,
        "totalCost": 107.50,
        "warningThreshold": 30,
        "lastUpdateTime": "2026-06-12 09:00:00"
      }
    ]
  }
}
```

## 库存预警查询

仓库管理员查询当前库存数量低于预警阈值的商品，并自动计算建议采购数量。关联商品表的 warning\_threshold，筛选出 stock\_quantity \< warning\_threshold 的商品。suggestPurchase 建议采购量可配置算法，Demo 版使用 \(warning\_threshold \- stock\_quantity\) \+ 常用消耗倍数，例如 \(阈值 \- 当前库存\) \+ 阈值

接口：`GET /api/v1/inventory/warning`

请求参数说明

响应

```JSON
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "total": 1,
    "pageNum": 1,
    "pageSize": 10,
    "list": [
      {
        "productId": 2,
        "productName": "瓶装洗发水",
        "stockQuantity": 25,
        "warningThreshold": 30,
        "suggestPurchase": 35
      }
    ]
  }
}
```

## 库存变动记录查询

财务人员、仓库管理员、总经理分页查询指定商品或时间范围内的所有库存变动流水，用于审计和追溯

按商品、时间区间、变动类型筛选。每条流水记录变动前后的库存量、关联单据号和操作人。

接口：`GET /api/v1/inventory/record`

请求参数说明



请求体

```JSON
{
  "page_num": 1,
  "page_size": 10,
  "product_id": 1,
  "change_type": 1,
  "start_time": "2026-06-01 00:00:00",
  "end_time": "2026-06-10 23:59:59"
}
```



响应

```JSON
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "total": 2,
    "pageNum": 1,
    "pageSize": 10,
    "list": [
      {
        "recordId": 1001,
        "productId": 1,
        "productName": "一次性牙具套装",
        "changeType": 1,
        "changeTypeName": "正常入库",
        "changeQuantity": 98,
        "beforeQuantity": 0,
        "afterQuantity": 98,
        "relatedNo": "RK-20260612-001",
        "operatorId": 1009,
        "operatorName": "李四",
        "remark": "验收合格",
        "createTime": "2026-06-12 10:30:00"
      },
      {
        "recordId": 1002,
        "productId": 1,
        "productName": "一次性牙具套装",
        "changeType": 2,
        "changeTypeName": "正常出库",
        "changeQuantity": -50,
        "beforeQuantity": 98,
        "afterQuantity": 48,
        "relatedNo": "LY-20260610-001",
        "operatorId": 1010,
        "operatorName": "王五",
        "remark": "已全部发货",
        "createTime": "2026-06-13 09:20:00"
      }
    ]
  }
}
```



## 补充指导



---



## 二、需补充的枚举与数据字典（统一管理）



### 2\.1 用户角色 \(`user.role`\)



|角色值|说明|
|---|---|
|`dept_employee`|部门员工（可发起领用/采购申请）|
|`dept_head`|部门负责人（审批本部门申请）|
|`purchaser`|采购员（确认采购、管理供应商）|
|`warehouse_keeper`|仓库管理员（入库、出库、库存查询）|
|`finance`|财务（查看报表）|
|`gm`|酒店总经理（查看所有数据）|



### 2\.2 商品分类 \(`product.category`\)



预定义分类（可扩展）：

- `客房用品`

- `食材`

- `工程物资`

- `办公用品`

- `清洁用品`

- `其他`

    

### 2\.3 采购单状态 \(`purchase_status`\)



|值|状态名|说明|
|---|---|---|
|1|待审批|部门员工刚提交，等待部门负责人审批|
|2|已通过（待采购）|审批通过，等待采购员确认下单|
|3|采购中|采购员已确认，已向供应商下单|
|4|已入库|仓库验收完成，增加库存|
|5|已取消<br>|被驳回或主动取消<br>|

修改为：

### 2\.4 入库单状态 \(`inbound_status`\)



|值|状态名|说明|
|---|---|---|
|1|待验收|仓库已创建单，等待部门负责人验收|
|2|已验收|部门负责人确认，库存已更新|



### 2\.5 入库类型 \(`inbound.inbound_type`\)



|值|类型名|说明|
|---|---|---|
|1|常规入库|进入仓库库存，计入库存数量|
|2|食材直拨|不经过仓库，直接消耗到部门，库存不增加|



### 2\.6 领用单状态 \(`receive.status`\)



### 2\.7 库存变动类型 \(`inventory_record.change_type`\)



|值|类型名|说明|
|---|---|---|
|1|正常入库|常规入库验收后增加库存|
|2|正常出库|领用出库确认后减少库存|
|3|食材直拨|直拨验收不计入库存，但生成负流水（成本计入部门）|
|4|期初初始化|系统上线时初始化库存|
|5|盘点调整|盘点后盘盈/盘亏调整|



---



## 三、各模块业务逻辑补充与冲突修正



### 3\.1 基础信息模块



**已明确**，无需补充。但建议增加一个**期初库存初始化接口**（见库存管理部分）。



### 3\.2 采购管理模块



#### 现有问题与补充



1. **采购单创建时的状态流转**

✅ 现有逻辑正确：`1-待审批` → 审批通过 → `2-已通过（待采购）` → 采购确认 → `3-采购中` → 入库验收 → `4-已入库`。



2. **采购单取消条件过于模糊** → 补充明确规则：  

    - 可取消的状态：`1（待审批）`、`2（已通过）`、`3（采购中）`。  

    - **不可取消条件**：已存在任何关联的入库单（`inbound` 表中 `purchase_id` 匹配），即使该入库单状态为 `待验收` 或 `已验收`。  

    - 操作权限：申请人本人或采购员可取消。

        

3. **审批时驳回** → 状态变为 6，并记录 `remark`。申请人可修改后重新提交（即创建新采购单）。

    

4. **采购确认** → 仅当状态为 `2-已通过` 时可执行，将状态变为 `3-采购中`。

    

5. **删除供应商的前置校验** → 若供应商已被任何采购单引用（`purchase.supplier_id`），则不可删除，应提示“已存在关联单据”。

    

### 3\.3 入库管理模块



#### 现有问题与补充



1. **创建入库单时，采购单状态限制**（文档未明确） → 补充规则：  

    - 采购单状态必须为 `3-采购中`（最合理）或至少为 `2-已通过`。  

    - **不能**为 `4-已入库` 或 `5-已取消`。  

    - 同一采购单可以创建多个入库单（支持分批入库）。

        

2. **入库单明细中的实际数量限制** → 单个商品本次 `actualQuantity` \+ 该采购单历史累计已入库数量（从 `inbound_item` 中按采购单汇总）≤ 采购单中的 `quantity`。  

    - 文档中 `purchase_item.received_quantity` 字段可用于累加，每次验收成功后更新该字段。

        

3. **验收确认时的业务逻辑细化**：

    - 校验 `deptHeadUserId` 必须是该采购单申请部门（`purchase.dept_id`）的负责人（`user.role='dept_head'` 且 `user.dept_id` 匹配）。

    - **常规入库（****`inbound_type=1`****）**：

        - 对每个明细，更新 `inventory` 表：

            - 原库存 `stock_quantity`、原总成本 `total_cost`。

            - 新入库金额 = `actualQuantity * unit_price`。

            - 新 `unit_cost` = \(原总成本 \+ 新入库金额\) / \(原库存 \+ actualQuantity\)（移动加权平均）。

            - 新 `stock_quantity` 增加，新 `total_cost` = 新库存 × 新 `unit_cost`。

        - 若商品从未在 `inventory` 中存在，则插入新记录。

    - **食材直拨（****`inbound_type=2`****）**：

        - **不更新 ****`inventory`**** 表**。

        - 生成一条 `inventory_record`，`change_type=3`，`change_quantity = -actualQuantity`（负数），`before_quantity` 和 `after_quantity` 均填 0（或保持原库存，但直拨不影响库存，可填 0）。

        - 成本直接计入部门（可通过后续财务报表统计采购单中的明细金额）。

    - 更新入库单状态为 `2-已验收`，记录 `dept_head_id` 和 `accept_time`。

    - 更新采购单状态为 `4-已入库`（若该采购单的所有商品均已入库完成，即累计入库数量等于采购数量；否则仍保持原状态。但为简化，可直接改为 `4-已入库` 并认为采购结束）。

    - 生成库存流水（`inventory_record`），`change_type` 根据 `inbound_type` 为 `1` 或 `3`，`related_no` = 入库单号。

        

4. **验收时不允许修改实际数量** → 数量在创建入库单时已确定，验收只做确认签字。

    

### 3\.4 领用出库管理模块



#### 现有问题与补充



1. **创建领用申请时** → 需校验 `deptId` 与 `applyUserId` 对应的部门一致，否则拒绝。

    

2. **领用单明细中的单价** → 创建时从 `inventory` 表获取当前 `unit_cost` 填入 `receive_item.unit_price`（快照），后续出库时以此单价扣减库存总金额。

    

3. **领用审批** → 部门负责人只能审批本部门的领用单（通过 `receive.dept_id` 匹配其 `dept_id`）。

    

4. **领用确认出库**（仓库管理员）细化：

    - 校验所有明细商品的 `inventory.stock_quantity` ≥ 领用数量。

    - 对每个商品：

        - 扣减 `stock_quantity`。

        - 扣减 `total_cost` = 领用数量 × 当前 `unit_cost`（注意：因为出库不改变 `unit_cost`，所以直接减）。

        - 注意移动加权平均的 `unit_cost` 在出库时不变。

    - 生成 `inventory_record`，`change_type=2`，`change_quantity = -领用数量`，`related_no` = 领用单号。

    - 更新领用单状态为 `3-已出库`，记录 `deliverer_id` 和 `deliver_time`。

    - 部门成本记录（可选）：可单独建一张部门成本表，或仅依赖库存流水后续汇总。

        

5. **取消领用单** → 仅当状态为 `1-待审批` 或 `2-待出库` 时可取消，变更为 `4-已取消`。若已出库则不可取消。

    

### 3\.5 库存管理模块



#### 现有问题与补充



1. **库存初始化** → 新增接口：`POST /api/v1/inventory/init`  

    - 角色限定：系统管理员。  

    - 请求体示例：

        ```JSON
        {
          "items": [
            {"productId": 1, "initialQuantity": 100, "unitCost": 2.50},
            {"productId": 2, "initialQuantity": 50, "unitCost": 4.00}
          ]
        }
        ```

    - 后台逻辑：插入/更新 `inventory` 表，并生成 `inventory_record`，`change_type=4`（期初初始化），`operator_id` 取当前操作人。

        

2. **移动加权平均计算** → 明确实现公式：

    - 新单价 = \(原总金额 \+ 本次入库金额\) / \(原库存数量 \+ 本次入库数量\)

    - 保留两位小数，四舍五入。

    - 示例：原库存 100，单价 2\.00，总金额 200；入库 50，单价 2\.20，金额 110；新库存 150，新总金额 310，新单价 = 310/150 ≈ 2\.07。

        

3. **库存预警接口** → 计算 `suggestPurchase` 的算法补充：  

    - 建议采购量 = \(预警阈值 \- 当前库存\) \+ 预警阈值 × 0\.5（向上取整），或简单使用 `(threshold - stock) + threshold`。文档示例中 `(30-25)+30=35`，可沿用此公式。

        

4. **库存变动记录查询** → 增加按 `related_no` 模糊查询，方便追溯单据。

    

---



## 四、补充缺失的接口（建议）



### 4\.1 库存初始化接口



```Plain Text
POST /api/v1/inventory/init
```



### 4\.2 商品分类枚举查询（供前端下拉选择）



```Plain Text
GET /api/v1/dict/categories
响应：["客房用品","食材","工程物资","办公用品","清洁用品","其他"]
```



### 4\.3 角色列表查询



```Plain Text
GET /api/v1/dict/roles
响应：[{"code":"dept_employee","name":"部门员工"}, ...]
```



---



## 五、数据库索引建议（提升查询效率）



```SQL
-- 采购单
ALTER TABLE `purchase` ADD INDEX idx_status (`status`);
ALTER TABLE `purchase` ADD INDEX idx_dept_id (`dept_id`);
ALTER TABLE `purchase` ADD INDEX idx_apply_user_id (`apply_user_id`);
ALTER TABLE `purchase` ADD INDEX idx_purchase_no (`purchase_no`);

-- 采购明细
ALTER TABLE `purchase_item` ADD INDEX idx_purchase_id (`purchase_id`);

-- 入库单
ALTER TABLE `inbound` ADD INDEX idx_purchase_id (`purchase_id`);
ALTER TABLE `inbound` ADD INDEX idx_status (`status`);
ALTER TABLE `inbound` ADD INDEX idx_inbound_no (`inbound_no`);

-- 领用单
ALTER TABLE `receive` ADD INDEX idx_dept_id (`dept_id`);
ALTER TABLE `receive` ADD INDEX idx_status (`status`);
ALTER TABLE `receive` ADD INDEX idx_apply_user_id (`apply_user_id`);
ALTER TABLE `receive` ADD INDEX idx_receive_no (`receive_no`);

-- 库存流水
ALTER TABLE `inventory_record` ADD INDEX idx_product_id (`product_id`);
ALTER TABLE `inventory_record` ADD INDEX idx_create_time (`create_time`);
ALTER TABLE `inventory_record` ADD INDEX idx_related_no (`related_no`);
```



---



## 六、开发路线总结



1. **基础信息模块**（优先）  

    - 用户、部门、供应商、商品 CRUD。  

    - 完成树形部门接口、用户分页筛选。

        

2. **采购模块**  

    - 采购申请（含自动生成单号） → 审批 → 采购确认 → 取消逻辑。  

    - 注意权限与状态机校验。

        

3. **入库模块**  

    - 创建入库单（依赖采购单） → 验收（核心：库存更新、移动加权平均、流水记录）。  

    - 区分常规入库与食材直拨。

        

4. **领用模块**  

    - 领用申请 → 审批 → 出库确认（库存扣减、流水）。

        

5. **库存管理模块**  

    - 库存查询、预警、流水查询、初始化接口。

        

6. **补充完善**  

    - 枚举查询接口、全局异常处理、统一响应格式。

        









